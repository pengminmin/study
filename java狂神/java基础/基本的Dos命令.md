1. 在任意文件夹下，按住 `Shift` + `鼠标右键` ，可用选择在此处打开 Powershell 窗口

2. 在文件导航栏最前面加 `cmd` + `空格` ，然后回车，可在当前文件夹位置打开 cmd 窗口

3. 跨盘切换

   ```bash
   cd /d D:\download
   ```

4. 创建文件

   ```bash
   cd > a.txt
   ```

   